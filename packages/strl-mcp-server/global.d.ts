// STRL: typecheck pulls the engine source (and thus @excalidraw source) in, so we
// need the same ambient augmentations the root build provides.
/// <reference types="vite/client" />
import "@excalidraw/excalidraw/global";
import "@excalidraw/excalidraw/css";
